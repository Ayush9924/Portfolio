/**
 * animations/gsap/index.ts — GSAP animation library
 *
 * Export reusable GSAP timeline factory functions and animation
 * presets from this file.
 *
 * Planned exports:
 *   - bootSequenceTimeline(container)   — staggered text-reveal on load
 *   - sectionRevealTimeline(el, config) — ScrollTrigger-driven entrance
 *   - glitchTimeline(target)            — CRT glitch effect
 *   - typewriterTimeline(el, text)      — terminal typing animation
 *
 * All functions should return a GSAP Timeline so callers can
 * `.play()`, `.reverse()`, or `.add()` them into larger sequences.
 */

// Animation factories will be added here.
// Example:
// export { bootSequenceTimeline } from "./bootSequence";
